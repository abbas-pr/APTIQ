# MongoDB connection string (local or Atlas)
MONGO_URI=mongodb://127.0.0.1:27017/quiz-app

# Server
PORT=5000

# JWT signing secret — use a long random string in production
JWT_SECRET=371196492ec90f939783f9041eaa366dbaf9a9a5

# Allowed frontend origin (Vite default)
CLIENT_URL=http://localhost:5173

# Optional: seed script creates admin with this email if not exists
ADMIN_EMAIL=abbas@gmail.com
ADMIN_PASSWORD=Abbas123!
